/**
 * Purpose: Domain-specific connection store exports for Root and Manager surfaces.
 */
export { connect, disconnect, connectionStatus, serverTime, state, timeOffset } from '../manager';
export type { ConnectionStatus } from '@shugu/sdk-manager';
